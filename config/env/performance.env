NODE_ENV=performance
PERFORMANCE_MODE=true
AUTH_MODE=local

# Standard password
PERFORMANCE_AUTH_PASSWORD=password123

POSTGRES_HOST=postgres_perf
POSTGRES_PORT=5432
POSTGRES_USER=admin
POSTGRES_PASSWORD=uFR44yr69C4mZa72g3JQ37GX
POSTGRES_DB=defaultdb

REDIS_HOST=redis_perf
REDIS_PORT=6379

# JWT Secret ----------------------------------------------------
JWT_SECRET=a-very-secure-performance-testing-jwt-secret-key-12345!@#$%

CACHE_DEFAULT_TTL=3600

# Redis cache optimization
REDIS_CACHE_MAX_MEMORY=256mb
REDIS_CACHE_POLICY=allkeys-lru
REDIS_CACHE_COMPRESS=true

# Mock server URL ----------------------------------------------
MOCK_GOOGLE_MAPS_URL=http://mock-google-maps:3000
# Google Maps Base URL for mock
GOOGLE_MAPS_BASE_URL=http://mock-google-maps:3000/maps/api

# API Keys ------------------------------------------------------
GOOGLE_MAPS_API_KEY=fake_api_key_for_performance_testing
FOURSQUARE_API_KEY=mock-perf-test-foursquare-key
ONFIDO_API_KEY=mock-perf-test-onfido-key

# Geohashing & Venue Scanning ----------------------------------
GEOHASH_PRECISION=7
VENUE_SCAN_RADIUS_METERS=5000
VENUE_SCAN_STALENESS_THRESHOLD_HOURS=720
VENUE_SCAN_WORKER_CONCURRENCY=3
VENUE_STALENESS_REFRESH_CRON='0 0 1 1 *'

# Image processing & static -------------------------------------------------
MAX_UPLOAD_SIZE=10485760
IMAGE_PROCESSING_QUALITY=85
IMAGE_PROCESSING_ASYNC=true
STATIC_FILE_CACHE_TTL=86400
STATIC_FILE_ETAG_ENABLED=true

# Monitoring ---------------------------------------------------
METRICS_ENABLED=true
METRICS_PORT=9090
HEALTH_CHECK_ENABLED=true
HEALTH_CHECK_TIMEOUT=5000 