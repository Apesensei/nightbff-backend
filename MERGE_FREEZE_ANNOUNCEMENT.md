# Merge Freeze Announcement – Digest-Driven Integration Migration (Phase 0)

We are freezing merges to `main` while we add GHCR publish workflows and migrate integration to image digests.

Scope:
- Add backend GHCR publish workflow (cosign + SBOM).
- Coordinate with frontend and integration repos.

Expected duration: 1–2 business days.

Allowed exceptions: docs/CI-only.
